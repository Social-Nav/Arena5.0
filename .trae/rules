禁止用 dummy/fallback 数据填补缺失的传感器状态；系统必须等待真实 TF/odom/camera 等 topic 就绪，否则 early exit / fail fast。

# 项目约束文件
在开始任何工作前，必须先阅读工作区根目录下的 AGENTS.md 文件（/home/ubuntu/arena_jazzy_ws/AGENTS.md），其中包含：
- Docker + Isaac Sim 执行环境硬性约束（禁止在宿主机运行 ROS/colcon/eval）
- 三容器职责：arena-1 运行 evaluator/ROS2/colcon，isaac-1 运行 Isaac Sim，internnav-1 才能运行真实 InternNav GPU 模型；真实模型 eval 必须使用 external-server 模式。
- GPU 残留进程清理流程
- Context7 MCP 文档查询约定
违反 AGENTS.md 中的约束可能导致环境损坏或无效操作。

# Run Case Launch 隔离规则
每个特定 run case 必须有自己的 `xxx.launch.py`。不要为了某个实验/评测场景去修改通用 `robot.launch.py` / `arena.launch.py` / `task_generator.launch.py` 的共享节点集合、启动顺序或初始化逻辑。

当某个 use case 需要不同 ROS nodes、需要禁用某些 nodes（例如 InternNav official realworld direct `cmd_vel` eval 需要完全不启动 Nav2）、需要额外 readiness gate 或不同初始化流程时，必须新增 case-specific launch 文件，并由评测/运行入口显式选择它（例如 `robot_launch_file:=internnav_async_eval.launch.py`）。

InternNav official realworld ROS2 client 的 async/direct `cmd_vel` eval 使用 `arena_simulation_setup/launch/internnav_async_eval.launch.py`，不得再通过修改通用 `robot.launch.py` 来跳过 Nav2，避免影响普通 Nav2 / Arena wrapper / DRL 等其他应用。

# InternNav async eval 启动入口硬性约束
InternNav async/direct `cmd_vel` eval 的入口是 `ros2 run arena_bringup internnav_eval ... --internnav-direct-cmd-vel`；该参数会自动设置 external-server 模式并向 `arena.launch.py` 追加 `robot_launch_file:=internnav_async_eval.launch.py`。不要只传 `--internnav-external-server` 后假设会选择 async launch；单独 external-server 是 legacy Nav2 service-contract 路径，仍可能经过 `robot.launch.py`。

若目标是当前 InternNav official realworld ROS2 client / async eval，必须同时满足：
- 命令在 `arena-arena_jazzy_ws-arena-1` 容器内执行，不能在宿主机执行 ROS/eval。
- 启动参数包含 `--internnav-direct-cmd-vel`，生成的 manifest / postprocess command 中必须出现 `robot_launch_file:=internnav_async_eval.launch.py`。
- `internnav_async_eval.launch.py` 不启动 Nav2、rosnav_rl 或本地 `dual_vln_server`；真实模型服务只能在 `arena-arena_jazzy_ws-internnav-1` 容器内运行。
- 如果日志或 manifest 中出现 `robot.launch.py` 且本次目标是 async/direct eval，应视为启动入口错误，先修正命令/文档，不要继续调试 `dual_vln_server` 缺失。

最小正确 eval 形态：
```bash
docker exec arena-arena_jazzy_ws-arena-1 bash -lc '
  cd /opt/arena_ws &&
  source /opt/ros/jazzy/setup.bash &&
  source install/setup.bash &&
  ros2 run arena_bringup internnav_eval \
    --sim isaac --human hunav --robot Ai2_Bot2 \
    --local-planner dual_vln \
    --internnav-mode internnav \
    --internnav-direct-cmd-vel
'
```

# GRScenes 天花板渲染可视化开关
GRScenes 室内 USD 场景的 `sim_top_down` debug 视频默认需要能透过天花板看到室内过程；相关逻辑位于 `arena_isaac/arena_isaac/isaac_utils/usd_scene_loader.py`，通过隐藏匹配到的 ceiling/roof `UsdGeom.Imageable` prim 实现，只影响渲染可见性，不删除 prim，也不修改 collision/physics。

可通过 `USDSceneLoader.load_scene(..., load_config=...)` 控制：
- `hide_ceiling_prims`: `true/false`，是否隐藏天花板/屋顶类 prim。
- `ceiling_name_patterns`: 字符串列表，大小写不敏感的 prim path/name 子串匹配规则；默认建议为 `['ceiling', 'ceil', 'roof']`。

也可通过 Isaac 运行环境变量控制：
- `ARENA_ISAAC_HIDE_CEILING_PRIMS=0/1`：全局关闭/开启天花板隐藏。
- `ARENA_ISAAC_CEILING_NAME_PATTERNS=ceiling,ceil,roof`：覆盖默认匹配规则，使用逗号分隔。

调试建议：
- 若 `sim_top_down.mp4` 被天花板完全遮挡，确认 `ARENA_ISAAC_HIDE_CEILING_PRIMS` 未被设为 `0`，并检查 `ceiling_name_patterns` 是否覆盖了当前资产中的 ceiling/roof 命名。
- 若需要验证原始资产天花板效果，可临时设置 `ARENA_ISAAC_HIDE_CEILING_PRIMS=0` 后重跑 eval；重跑前仍必须遵守 AGENTS.md 的 Isaac/eval 残留进程清理流程。
